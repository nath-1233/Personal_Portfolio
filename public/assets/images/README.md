# Profile image

Add the portfolio owner's photo here as `profile-picture.jpg` (or update
`profileImage` in `lib/site-data.ts`). The hero shows the owner's initials if
the image is missing, so the site remains usable before a photo is added.
